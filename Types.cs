namespace Announcer
{
    public enum StreamType { twitch, hitbox, beam, youtube, goodgame, cybergame, bargonia }

    public enum DataType { giantbomb, tmdb }

    public enum ServiceType { announce, stream, media }

    public enum ChannelType { game, movies }

    public enum AnnounceType { push, vk, discord }
}